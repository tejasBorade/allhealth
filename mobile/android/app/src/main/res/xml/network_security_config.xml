<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows plain-http traffic only to local dev hosts, so `APP_BASE_URL` can
  point at a `next dev` server (default http://10.0.2.2:3000, the Android
  emulator's alias for the host machine) without Android 9+'s default
  cleartext block. Everything else — including the production deployment —
  still requires https.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
